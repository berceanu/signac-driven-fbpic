from setuptools import setup

setup(
   name='postproc',
   version='0.0.1',
   author='Andrei Berceanu',
   author_email='andrei.berceanu@eli-np.ro',
   packages=['postproc'],
   description='Package containing unused modules',
   install_requires=[],
)
